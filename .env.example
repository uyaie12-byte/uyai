# Optional: point newsletter/waitlist signups at a real ESP or automation.
# When set, every /api/subscribe submission is also POSTed here as JSON:
# { "email": "...", "source": "notify|home|article|footer|the-camp", "createdAt": "..." }
# Point this at a Zapier/Make webhook, an Airtable/Google Sheets automation,
# or a Beehiiv/ConvertKit/Mailchimp webhook once one is chosen (Skill §9).
SUBSCRIBE_WEBHOOK_URL=
